# 多磨霊園のお墓相談比較ガイド
# 公開情報の比較ガイドとして、主要ページのクロールを妨げない方針。
# 管理用パス等は現時点で存在しないため、個別のDisallowは設定していない。
# OAI-SearchBotを含め、特定クローラーを意図的にブロックしていない。

User-agent: *
Allow: /

# 実際のsitemap生成はPhase 1のスコープ外（@astrojs/sitemap等が未導入）。
# QA監査D-3（2026-08-26）: 未生成のsitemap-index.xmlをSitemap:行で参照すると
# 本番公開時に404となりクロールエラーの原因になるため、生成されるまでの間は
# 行自体を記載しない。sitemap生成を導入する際に本行を復活させ、
# 本番origin確定後の絶対URLへ更新すること。
