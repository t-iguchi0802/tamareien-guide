# 多磨霊園のお墓相談比較ガイド
# 公開情報の比較ガイドとして、主要ページのクロールを妨げない方針。
# 管理用パス等は現時点で存在しないため、個別のDisallowは設定していない。
# OAI-SearchBotを含め、特定クローラーを意図的にブロックしていない。

User-agent: *
Allow: /

# 実際のsitemap生成はPhase 1のスコープ外。
# パスのみ仮に記載（本番origin確定後に絶対URLへ更新が必要）。
Sitemap: /sitemap-index.xml
