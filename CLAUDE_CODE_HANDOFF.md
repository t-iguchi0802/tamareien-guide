# Claude Code制作引継書｜多磨霊園のお墓相談比較ガイド

- 作成日：2026年8月24日
- 制作方式：Claude Codeの複数エージェント＋Git worktree
- 初期公開方式：静的サイト
- データ方式：JSON＋Markdown。外部データベースは導入しない

## 1. 制作目的

多磨霊園周辺の石材店について、墓じまい、墓所管理、清掃、納骨、修理等を、公開情報で中立に比較できる地域サイトを制作する。

サイト名：**多磨霊園のお墓相談比較ガイド**

サブタイトル：**墓じまい・管理・納骨・石材店を公開情報で比較**

### 非公式表記（2026-08-27改定。詳細はCLAUDE.md 2章「表記・信頼性」）

**変更**: 「ヘッダー、フッター、運営者情報に次を表示する」という当初のルールは、
サイト所有者の判断により2026-08-27付で廃止した。変更理由（記録）:
サイト所有者の判断により、非公式表記の常時表示から、誤認を生じさせない
サイト設計と運営情報・出典の明示へ方針を変更。

現行ルール（CLAUDE.md 2章と同一。本ドキュメントとCLAUDE.mdが矛盾しないよう、
ここにも同じ内容を記載する）:

- 全ページのヘッダー、本文上部、フッターへ「公式サイトではありません」という
  注意書きを常時表示しない。黄色やベージュ等の非公式注意書き帯も設置しない。
- 東京都、多磨霊園、東京都公園協会のロゴを無断使用しない。
- サイト名・説明文で「公式」と名乗らない。東京都等が運営していると
  誤認させる表現を使用しない。
- 運営者情報・掲載方針・情報源・訂正窓口は、専用ページ（`/about/` `/sources/`）
  で確認できるようにする。フッターにはこれらのページへのリンクを置くが、
  非公式である旨の文章そのものは常時表示しない。
- 公式情報を扱う記事には、情報源・確認日・公式ページへのリンクを掲載する。
- 公式資料の本文・地図画像を無断転載しない（要約またはリンクで扱う）。

## 2. 制作前提

- 事業者への事前ヒアリング、スポンサー営業、掲載許可待ちは行わない。
- 公開情報で確認できない項目は「公開情報では未確認」と表示する。
- 条件未選択時はスポンサー契約と通常掲載順を分離する。サービス条件検索時だけ、条件に一致するスポンサー契約店を「PR」表示付きで先頭表示する。
- 口コミランキング、独自点数、根拠のないおすすめ表示は作らない。
- 東京都・多磨霊園公式を模倣する名称、ロゴ、配色、ドメインを使用しない。
- 正確な検索数は未確認。検索ボリュームを断定する文章を作らない。

## 3. 推奨技術構成

- Astro＋TypeScript
- 静的出力
- Cloudflare Pages
- JSONデータ：`src/data/businesses.json`
- 記事：Astro Content CollectionsまたはMarkdown/MDX
- CSS：CSS Modules、Tailwind CSSのどちらでもよいが、共通方式をリードが先に固定する
- テスト：Vitest＋Playwright＋axe相当のアクセシビリティ確認
- 外部DB、会員ログイン、口コミ投稿はMVP対象外

バージョン番号はこの文書に固定せず、着手時の安定版を採用しロックファイルで固定する。

## 4. 入力ファイル

| ファイル | 用途 | 取扱い |
|---|---|---|
| `businesses.json` | 掲載事業者データ | 事実データ。件数はデータから自動表示する |
| `data_dictionary.md` | 状態値・項目・表示ルール | UIと検証処理の契約 |
| `sources.csv` | 出典、確認日、根拠 | 出典ページ・更新管理に使用 |
| `WORKTREE_AGENT_PLAN.md` | 並列開発・担当範囲 | リードが厳守させる |

## 5. MVPのページ

| URL案 | ページ |
|---|---|
| `/` | トップ・困りごと別入口 |
| `/sekizaiten/` | 掲載事業者の一覧・比較表 |
| `/hakajimai/` | 墓じまい・墓石撤去の比較と見積項目 |
| `/kanri/` | 墓所管理・清掃・供花の比較 |
| `/tetsuzuki/` | 改葬・墓所返還・施設変更の違いと公式リンク |
| `/about/` | 運営方針、掲載基準、広告方針、訂正窓口 |
| `/sources/` | 情報源・最終確認日 |
| `/privacy/` | プライバシーポリシー |

初期は薄い事業者個別ページを14枚量産しない。一覧比較で十分な情報があるA候補から、後続フェーズで詳細ページを追加する。

## 6. 機能要件

### 比較表

- 地域・門で絞り込み
- サービスで絞り込み
- 五十音または管理順で並べ替え
- `confirmed`だけを対応確認済みとして絞り込み対象にする
- `not_confirmed`は「公開情報では未確認」と表示する
- 公開価格は条件・単位・税込区分を併記する
- スマートフォンではカード表示または横スクロール＋項目固定を採用する

### 事業者導線

- 公式サイト
- 電話
- 地図検索
- 問い合わせページ

リンククリック時に事業者IDと導線種別を計測する。電話番号、地図、公式サイトURLが未確認の場合はボタンを作らない。

### 訂正窓口

- 掲載情報の訂正申請
- 事業者名
- 訂正対象
- 正しい情報
- 根拠URL
- 連絡先

スポンサー契約の有無に関係なく訂正を受け付ける旨を表示する。フォーム実装前はメールリンクでもよい。

## 7. 計測仕様

### GA4イベント名

| イベント | 必須パラメータ |
|---|---|
| `business_outbound_click` | `business_id`, `business_name`, `link_type`, `page_path` |
| `business_phone_click` | `business_id`, `business_name`, `page_path` |
| `business_map_click` | `business_id`, `business_name`, `page_path` |
| `comparison_filter_use` | `filter_type`, `filter_value`, `page_path` |
| `official_procedure_click` | `authority`, `topic`, `page_path` |
| `correction_request_click` | `page_path` |

実IDは環境変数で設定し、リポジトリに直書きしない。Cookie・同意表示の要否は導入する計測方式に応じて公開前に確認する。

## 8. SEO要件

- ページごとに固有の`title`、`description`、canonical
- XMLサイトマップ
- robots.txt
- OGP・Xカード
- BreadcrumbList
- 一覧ページはItemListを検討
- 個別事業者を構造化する場合は、画面に表示している確認済み情報だけを使用する
- review、aggregateRating、award、best等のマークアップは使用しない
- 行政制度ページは東京都公園協会・府中市の公式URLへリンクする
- 記事と比較表に最終確認日を表示する
- 「多摩霊園」ではなく正式名称の「多磨霊園」を基本表記とする

## 9. デザイン要件

- 高齢者本人だけでなく、離れて暮らす40〜60代の家族が読みやすいこと
- 落ち着いた白・深緑・石色を基調とするが、東京都公式の外観を模倣しない
- 本文16px未満を避ける
- 十分なコントラスト、タップ領域、行間
- 墓石・霊園写真を使う場合は、権利確認済み素材だけを使用する
- 不安を過度に煽る表現、遺族を急かすCTAを使用しない

## 10. データ検証

ビルド前に次を自動検証する。

1. 事業者IDとslugの重複がない。
2. `displayOrder`が重複していない。
3. `services`に全サービスキーが存在する。
4. 状態値が許可リスト内にある。
5. `evidenceSourceIds`が`sources.csv`に存在する。
6. `publishedPrices.sourceId`が`sources.csv`に存在する。
7. URL形式が正しい。
8. `confirmed`の重要表示に少なくとも1つの根拠がある。
9. `not_confirmed`が「非対応」に変換されていない。

## 11. 受入条件

- `npm run build`が成功する
- TypeScriptエラーがない
- 全内部リンクが有効
- 主要ページを360px、768px、1440pxで確認
- キーボード操作が可能
- axe相当の重大エラーがない
- LighthouseのPerformance、Accessibility、Best Practices、SEOを確認し、重大な阻害要因を解消
- 掲載事業者がデータから生成され、HTMLへ手書き複製されていない
- 未確認情報が断定表示されていない
- 運営者情報（`/about/`）、掲載基準、広告方針、訂正窓口がある（2026-08-27改定:
  非公式表記の常時表示バナーは廃止。詳細は本ドキュメント冒頭の
  「非公式表記」節・CLAUDE.md 2章参照）
- GA4未設定でもサイトが壊れない

## 12. MVP対象外

- 口コミ投稿
- 会員登録
- 事業者ログイン
- 見積り一括送信
- 課金・請求
- Supabase等の外部DB
- AIチャット相談
- 自動スクレイピングによる無審査更新

## 13. 完成時の引渡し

リードエージェントは次を提出する。

- 統合済みGitブランチ
- README
- 環境変数の例
- Cloudflare Pagesの設定手順
- テスト結果
- 未解決事項
- 公開前チェックリスト
- 各worktree・ブランチの処理状況
